import React from 'react';
import {Grid, Row, Col, Button} from 'react-bootstrap';

const boardNominees = [
	{
		name: 'Jim Hewitt',
		organisation: 'EOS UK',
		selfNominated: false,
		nominatedBy: [
			'',
			''
		],
		bio: '',

	}
];

const Board = () => {
	return (
		<div>board</div>
	);
};

export default Board;