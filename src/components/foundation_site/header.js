import React from 'react';
import {Grid, Row, Col} from 'react-bootstrap';

const Header = () => {
	return (
		<header className='foundation_header'>
			foundation header
		</header>
	);
};

export default Header;